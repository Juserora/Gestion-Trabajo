# Document Extraction Pipeline - dependencias
# Nota: pdf2image requiere 'poppler' instalado a nivel de sistema (NO via pip).
#   Ubuntu/WSL: sudo apt-get install poppler-utils
#   macOS:      brew install poppler
anthropic>=0.39.0
pydantic>=2.5.0
pdf2image>=1.17.0
Pillow>=10.2.0
pytest>=8.0.0
